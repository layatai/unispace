# UniSpace creates only virtual keyboard/pointer devices. Membership in the
# dedicated group is explicit because /dev/uinput permits system-wide input.
KERNEL=="uinput", GROUP="wheel", MODE="0660", OPTIONS+="static_node=uinput"
